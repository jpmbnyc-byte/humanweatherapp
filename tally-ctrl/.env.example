# Optional — franchise seeding + live preset economics.
# Thumbnails ship as same-origin /cars/*.jpg; Gemini image only on Refresh.
VITE_GEMINI_API_KEY=
VITE_GEMINI_MODEL=gemini-2.0-flash
VITE_GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
# Host used when composing /mint send links (no protocol)
VITE_PREVIEW_HOST=preview.tallyctrl.com
